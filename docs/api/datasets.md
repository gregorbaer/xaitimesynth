# Datasets

Ready-made dataset generators. Each function returns the standard xaitimesynth dictionary with ground-truth feature masks included.

::: xaitimesynth.datasets.generate_cylinder_bell_funnel
