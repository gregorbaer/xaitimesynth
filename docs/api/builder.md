# Builder

The `TimeSeriesBuilder` class provides a fluent API for constructing synthetic time series datasets.

## TimeSeriesBuilder

::: xaitimesynth.builder.TimeSeriesBuilder
    options:
      members:
        - __init__
        - for_class
        - add_signal
        - add_feature
        - build
        - clone
        - to_df
        - convert_data_format
      show_root_heading: true
      heading_level: 3
